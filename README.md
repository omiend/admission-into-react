# Admission into React

## install

```
$ git clone git@github.com:omiend/admission-into-react.git
$ cd admission-into-react
```

## build
```
$ npm install
```

## run
```
$ npm start
```

go to `http://localhost:3000`

## build 2
```
$ webpack --progress --color --watch
```
